import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Customers',
  description: 'The official Next.js Learn Customers Page built with App Router.',
};

export default function Page() {
  return <p>Customers Page</p>;
}